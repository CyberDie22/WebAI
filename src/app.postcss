/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

input[type=range] {
    background: linear-gradient(0deg, transparent 30%, rgb(255 255 255 / 0.1) 50%, transparent 70%);
}